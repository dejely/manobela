# Deployment

## Backend with Docker

- Build and run with docker-compose in backend/.
- The container exposes port 8000 and reads backend/.env.
- Set PORT if you want a non-default port.
- Provide Metered TURN credentials for /turn-usage and ICE server discovery.
